[plugin]

name="GameDatabase"
description="Game-agnostic designer-data core: DataItem (StringName-id base resource), GameDatabase (baked, lazily-indexed id->item index per type), DatabaseConfig (which types, which folders, output path) and DatabaseGenerator (config-driven folder scan that bakes the database). Zero game types."
author="DiabloHumaStudio"
version="1.0.0"
script="game_database_plugin.gd"
